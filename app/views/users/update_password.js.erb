$('.text-danger').remove()
<% if @errors.nil? %>
<% else %>
  <% @errors.each do |key, value| %>
    $('#user_<%= key %>').after('<%= contextual t(".#{key}") + ' ' + value.last %>')
  <% end %>
<% end %>
$('#notifications').html('<%= alert_bs @errors.class %>')

